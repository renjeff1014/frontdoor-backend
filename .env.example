PORT=3001
JWT_SECRET=your-super-secret-key-change-in-production
CORS_ORIGIN=http://localhost:5173

# PostgreSQL (used when running with Docker Compose or a real DB)
DATABASE_URL=postgresql://frontdoor:frontdoor_dev@localhost:5432/frontdoor

# SMTP for verification emails (optional; if unset, code is logged to console only)
# Gmail: use smtp.gmail.com, port 587, and an App Password (not your normal password)
#        https://myaccount.google.com/apppasswords
# Leave SMTP_HOST empty or comment out to skip sending email (code still logged in console)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# MAIL_FROM=your-email@gmail.com
